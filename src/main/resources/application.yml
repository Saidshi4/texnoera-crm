server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://ns-course-database-do-user-16333189-0.d.db.ondigitalocean.com:25060/defaultdb
    username: doadmin
    password: AVNS_QycABlcMxGk4MZ-wS1G
    hikari:
      maximum-pool-size: 20
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: liquibase/change-log-master.yaml

