server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://ns-course-database-do-user-16333189-0.d.db.ondigitalocean.com:25060/defaultdb
    username: doadmin
    password: AVNS_QycABlcMxGk4MZ-wS1G
    hikari:
      maximum-pool-size: 20  # Maksimum 20 bağlantı
      connection-timeout: 30000  # Timeout 30 saniye (milisaniye cinsinden)

  jpa:
    hibernate:
      ddl-auto: update  # Liquibase kullanıyorsanız, Hibernate'in şema yönetimi yapmasını istemezsiniz.
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    enabled: false
    change-log: classpath:db/changelog/db.changelog-master.xml  # Liquibase için ana değişiklik dosyası
